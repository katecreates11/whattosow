# The SEO Watchdog — weekly SEO health

**Date:** 4 August 2026
**Scope:** repo audit + live site + web search. No Google Search Console access yet — every indexing claim below is approximate; a GSC connection would sharpen all of it (see note at the end).

---

## Before anything else: nothing shipped last week

`src/app/` and `src/data/` are **byte-identical** to the state they were in when the 28 July report was written (verified with `git diff origin/seo/2026-07-28 origin/main -- src/app src/data`, zero lines). The only commits since then touched `docs/dreams/` and `docs/ideas-board.md` — content-pipeline bookkeeping, not the site. That means every item in last week's FIX NOW section is still open, unverified line-by-line but spot-checked directly on the exact files below. Rather than re-paste the same six items, this report marks each **[STILL OPEN]** and only gives full detail to what's new or changed.

---

## FIX NOW

### 1. [STILL OPEN] All 51 `/sow-in/[city]` pages build their title/description from `new Date()` at build time
`src/app/sow-in/[city]/page.tsx:39` — no `revalidate` export. Confirmed unchanged since 28 July; still showing "July 2026" in title/meta on every city page regardless of when a reader visits, and it will keep drifting wrong until the next full rebuild. **Fix:** add `export const revalidate` (daily/monthly) matching `src/app/crops/[slug]/page.tsx`'s pattern.

### 2. [STILL OPEN] FAQPage schema missing on 45 of 48 crop pages
`src/app/crops/[slug]/page.tsx:477` only emits FAQ JSON-LD when a `crop-playbooks.ts` entry exists (still just tomatoes, carrots, courgettes). Confirmed unchanged.

### 3. [STILL OPEN] Crop-page meta descriptions run over ~160 chars
Both the generic and playbook templates in `src/app/crops/[slug]/page.tsx:430` are unchanged — playbook descriptions still 175-178 chars, generic template still overflows on longer crop names.

### 4. [STILL OPEN] Playbook crop titles (tomatoes/carrots/courgettes) run 77-94 chars vs. ~60 chars for the other 45 crops.

### 5. [STILL OPEN] Variety page titles repeat the crop name awkwardly for compound names, e.g. "Purple Sprouting Broccoli Early purple sprouting broccoli — how to grow it" (88 chars). Lower priority, 181 pages affected.

### 6. [STILL OPEN] `keywords` meta arrays present in 54 files
Confirmed still 54 files via the same grep as last week — no change. Per `docs/CUT_LIST.md` §4, stop adding to new pages; a bulk-strip pass would clear this in one go.

### 7. NEW — `/us` (the US ZIP-code beta) has zero inbound internal links, and it wasn't flagged before
`src/app/us/page.tsx` is fully indexable (no `noindex`, canonical set, priority 0.7/weekly in the sitemap) but **no page on the site links to it** — not the homepage, not the footer, not any crop page. `src/lib/us-visitor.ts` has geo-detection logic that *recognises* when someone is already on `/us`, but nothing proactively surfaces the page to a UK-side crawler or a US visitor landing anywhere else on the site. Unlike `/sow-in` or `/still-time`, this orphan isn't a deliberate CUT_LIST hold — it just has no doorway. **Fix:** add at least one real link — a small footer mention ("Gardening in the US? →") or a link from the homepage's geo-detection path — so Google has a path to it that isn't the sitemap alone.

### Unchanged and healthy (re-verified, not just carried forward)
Canonical tags: still clean and consistent (`alternates.canonical` resolved against `metadataBase`). Headings: still one clean H1 per template, no live heading-stutter. `/print` is genuinely linked (from `/calendar` and `/products`, 2 inbound links) — see correction below.

---

## INDEXING

Approximate — no Search Console access. Two checks: internal-linking health (repo-based, reliable) and apparent Google indexing (web search, soft signal).

### Orphan pages — one correction, one new finding, rest unchanged

- **Correction to last week: `/print` is NOT orphaned.** Last week's report listed it as orphaned "by design." Re-checked this week with a full grep pass — it's linked from `src/app/calendar/page.tsx:74` ("Print a sowing chart →") and `src/app/products/page.tsx:128`. Two real inbound links from indexable pages. No action needed; correcting the record.
- **NEW: `/us` — zero inbound links, not a deliberate hold** (see FIX NOW #7 above). This is the one orphan this week that's a plain gap rather than a considered wait-and-see.
- **`/sow-in` hub + 51 city pages, `/still-time`** — still intentionally unlinked, still guarded by `search-console-readiness.test.ts`, still the correct call pending real indexing data. No change.
- **`/grow-your-christmas-dinner`** — re-confirmed zero inbound links (checked directly this week, not just carried forward). Still not on any CUT_LIST hold — still a plain oversight, and still low urgency in August, but worth doing before the page's own season (link it in from one seasonal guide or `/sow/december` when autumn content gets touched).

Everything else re-spot-checked clean: `/frost-map` (13 inbound links, in both header and footer), `/blight-watch` and `/harvest-planner` (5 inbound links each, `/blight-watch` in the header "More" menu), `/longest-day`, `/about`, `/guides/seed-starting-kit`, `/guides/companion-planting-chart` — all reachable from multiple real pages, none needing action.

### Apparent indexing (web search, approximate)

Same caveat as last week — `site:` searches on this domain are unreliable through this tool (results drift to unrelated Wikipedia/third-party pages even for exact-match queries). Cross-checking without the operator:

- `/blight-watch` surfaces indirectly (city pages reference "bookmark and check back" blight copy) but the page itself didn't show up directly — same as last week, still no clean signal either way.
- `/grow-your-christmas-dinner` and `/guides/sow-now-eat-all-winter` — still no direct hit, one week on from first being flagged. Given both are also orphaned or were only recently linked, this is consistent with "not indexed yet" rather than a search-tool artefact.
- `/guides/watering-while-away` — no direct hit; the query surfaced only third-party watering-while-away advice, no whattosow.co.uk result at all.

**Recommendation, unchanged from last week and now more pressing:** `/grow-your-christmas-dinner` has had zero inbound links for at least two weeks running, which independently explains the lack of indexing signal — an orphan page is exactly the kind of page Google is slow to crawl. Linking it in (see FIX NOW-adjacent note above) would very plausibly fix both problems, indexing and traffic, in one change. This is the clearest case this week for what a Search Console "not indexed" pull would confirm outright instead of inferring.

---

## OPPORTUNITIES

Two of last week's three are still open with less runway; one new item below reflects what's actually moving in UK gardening news right now.

1. **National Allotments Week, 10–16 August 2026 — six days out, still zero site coverage.** Confirmed dates this week (National Allotment Society, theme "Grow to Learn"). This was already an approved-but-unbuilt idea-board card (approved 11 July, per `docs/dreams/wisdom.md`) and last week's SEO report flagged it independently too — it's now been open for over three weeks with the window about to close. If it isn't going to ship before the 10th, the SEO case for it effectively expires this week.

2. **Hosepipe bans / drought — still live, still zero dedicated page.** Web search this week confirms water restrictions remain "a significant concern for UK gardeners" into August 2026, with drought-tolerant gardening advice actively being published elsewhere. `docs/dreams/wisdom.md` records a hosepipe-ban idea-board card approved 22 July — also still unbuilt per this week's zero-diff check (`grep -rl hosepipe src/` finds it only in one editorial post and the general `/guides/watering` page, nothing dedicated). Third week this gap has been visible from two independent sources (the ideas board and this audit); the site's only current watering content doesn't address the "what am I actually allowed to do" question people are searching right now.

3. **Maincrop potato lifting timing — near-miss on `/crops/potatoes`, still unaddressed.** Repeating last week's finding because the window is now open, not closing: search results this week confirm late August is exactly when gardeners start looking for "when do I lift maincrop potatoes" (foliage die-back, then a 10-14 day wait before lifting for storage). `/crops/potatoes` already carries buying advice but nothing in `crops.ts` covers this. Still one paragraph away from being answered on a page the site already has some authority on.

**Not repeating the sweetcorn-harvest opportunity from last week** — no evidence it's more or less urgent this week than last, and repeating unbuilt items without new evidence just pads the list; it's still valid but the three above are the sharper picks for this exact week.

---

## HEALTH SUMMARY

- **Zero code or content changes shipped to `src/app` or `src/data` since the 28 July report.** This is itself the top-line finding this week — every previously-flagged technical fix is still outstanding, and two evidence-backed seasonal opportunities (Allotments Week, hosepipe bans) have now been visible for two to three weeks without shipping, with Allotments Week's window closing in 6 days.
- **~400 URLs** in `src/app/sitemap.ts`, unchanged: 48 crop pages, 182 variety pages, 51 city pages, 32 guides, ~38 blog/editorial posts, 19 companion-planting topic pages, 12 month pages, plus standalone utility/core pages.
- **Titles/descriptions:** 0 missing; ~48 crop pages still have oversized meta descriptions; 3 playbook titles still oversized; 51 city pages still carry the stale build-time date bug.
- **Canonical tags:** clean.
- **Structured data:** FAQPage still missing on 45/48 crop pages; everything else (guides, blog, crop/variety/city breadcrumbs) still in good shape.
- **Headings:** clean, no regressions found.
- **`keywords` arrays:** still 54 files, unchanged.
- **Orphan pages:** 1 correction (`/print` is not orphaned — was misreported last week), 1 new unflagged orphan (`/us`), 2 known intentional holds unchanged (`/sow-in` + cities, `/still-time`), 1 likely-oversight orphan re-confirmed (`/grow-your-christmas-dinner`).
- **Indexing:** no clean positive signal this week on the same handful of newer/orphaned pages flagged last week; the pattern (unlinked pages = no signal) is now consistent across two weeks of checking, which is itself weak evidence the orphan-page theory is right, not just noise.
- **Opportunities:** 3 flagged, 2 carried forward with new urgency (one has a hard deadline in 6 days), 1 re-confirmed as still open with the window now current.

---

### On the Search Console gap

Unchanged from last week: every "approximate" and "no signal either way" above stands in for a five-minute Search Console check. This week sharpens the case further — `/grow-your-christmas-dinner`'s orphan status and its lack of indexing signal are almost certainly the same fact seen two ways, and GSC would confirm that outright instead of leaving it as a plausible inference.
